import React from 'react';
import {
	View,
	Text,
	StyleSheet
} from 'react-native';

const GameResultScreen = props => {

	return(
		<View>
			<Text>GameResultScreen Screen</Text>
		</View>
	);
};

const styles = StyleSheet.create({
	
});

export default GameResultScreen;