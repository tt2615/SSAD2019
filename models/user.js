class User {
	constructor(userId, password, userName, totalPoint, character){
		this.userId = userId;
		this.password = password;
		this.userName = userName;
		this.totalPoint = totalPoint;
		this.character = character;
	}
}

export default User;